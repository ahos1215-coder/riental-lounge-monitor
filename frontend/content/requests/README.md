Blog Requests (contract examples)

This folder stores sample request payloads that document the minimal schema.
It is not the production storage location.

Naming:

sample_<store>_<kind>_<yyyymmdd>.json for examples

<request_id>.json if you want to mirror real request IDs

Source of truth: plan/BLOG_REQUEST_SCHEMA.md.
