diff --git a/frontend/src/components/PreviewMainSection.tsx b/frontend/src/components/PreviewMainSection.tsx
index ff55502..f3c0d1a 100644
--- a/frontend/src/components/PreviewMainSection.tsx
+++ b/frontend/src/components/PreviewMainSection.tsx
@@ -1,6 +1,6 @@
 "use client";
 
-import { useEffect, useMemo, useState } from "react";
+import { useEffect, useMemo, useRef, useState } from "react";
 import {
   Area,
   AreaChart,
@@ -92,6 +92,8 @@ type PreviewMainSectionProps = {
   selectedBaseDate?: string;
 };
 
+type DateInputWithPicker = HTMLInputElement & { showPicker?: () => void };
+
 export default function PreviewMainSection(props: PreviewMainSectionProps) {
   const {
     storeSlug,
@@ -109,6 +111,20 @@ export default function PreviewMainSection(props: PreviewMainSectionProps) {
   const hasData = snapshot.hasData;
   const activeRangeMode = rangeMode ?? "today";
   const canControlRange = typeof onChangeRangeMode === "function";
+  const dateInputRef = useRef<HTMLInputElement | null>(null);
+
+  const openDatePicker = () => {
+    const el = dateInputRef.current as DateInputWithPicker | null;
+    if (!el) return;
+    // focus だけだと開かないブラウザもあるため、showPicker があれば使う（Chrome 等）
+    el.focus();
+    el.showPicker?.();
+  };
 
   const [isClient, setIsClient] = useState(false);
   useEffect(() => setIsClient(true), []);
@@ -155,13 +171,26 @@ export default function PreviewMainSection(props: PreviewMainSectionProps) {
                   return (
                     <button
                       key={opt.id}
                       type="button"
-                      onClick={() => onChangeRangeMode(opt.id)}
+                      onClick={() => {
+                        onChangeRangeMode(opt.id);
+                        if (opt.id === "custom") {
+                          // DOM反映後に picker を開く
+                          setTimeout(() => openDatePicker(), 0);
+                        }
+                      }}
                       className={[
                         "rounded-full border px-3 py-1 text-[11px] font-semibold transition",
                         active
                           ? "border-amber-300/80 bg-amber-400/10 text-amber-100"
                           : "border-slate-700 bg-slate-950 text-slate-200 hover:border-slate-500",
                       ].join(" ")}
                     >
                       {opt.label}
                     </button>
                   );
                 })}
               </div>
 
               <div className="flex flex-wrap items-center gap-2 md:ml-auto">
-                <input
-                  type="date"
-                  value={customDate}
-                  onChange={(e) => {
-                    const next = e.target.value;
-                    onChangeCustomDate?.(next);
-                    onChangeRangeMode("custom");
-                  }}
-                  className="rounded-md border border-slate-700 bg-slate-950 px-2 py-1 text-[11px] text-slate-200"
-                />
+                {activeRangeMode === "custom" && (
+                  <div className="flex items-center gap-2">
+                    <span className="text-[11px] text-slate-500">日付</span>
+                    <input
+                      ref={dateInputRef}
+                      type="date"
+                      value={customDate}
+                      onChange={(e) => {
+                        const next = e.target.value;
+                        onChangeCustomDate?.(next);
+                        onChangeRangeMode("custom");
+                      }}
+                      className="rounded-md border border-slate-700 bg-slate-950 px-2 py-1 text-[11px] text-slate-200"
+                    />
+                  </div>
+                )}
                 {selectedBaseDate && (
                   <span className="text-[11px] text-slate-500">
                     表示: {selectedBaseDate}（19:00-05:00）
                   </span>
                 )}
               </div>
             </div>
           </div>
         )}
