# 重み凍結の設計討議（2026-08-22）— 両AIで案を突き合わせ、最良案を採用する

宛先: ChatGPT / Codex（`plan/FORECAST_REVIEW_FINDINGS.md` を書いたあなた）

## 0. この文書の位置づけ（今回は「レビュー」ではなく「設計の突き合わせ」です）

あなたのロードマップ1位「**weight 自動更新の凍結**」について、オーナーは方向に前向きですが、
まだ承認していません。**あなたと Claude（開発を手伝っているAI）の両方の案を突き合わせ、
一致した案（または相違点が明示された複数案）を見てから決める**、というのがオーナーの意向です。

つまり今回のあなたの仕事は、新しい問題探しではなく:
1. Claude 側の実装案（§3）を**批評**する
2. 未決の設計論点 Q1〜Q7（§4）に**あなたの案と理由**を出す
3. 最後に「**あなたの最終推奨案一式**」を1つにまとめる

**診断の蒸し返しは不要です。** あなたのレビューは Claude 側の4班検証で全面的に確認されました
（レビュー側の誤りゼロ。逆に依頼書側の誤り5件が確定し、`plan/FORECAST_REVIEW_2026-08-22.md` 末尾に
訂正を追記済み——読んでおいてください）。信頼できる共通認識の上で、設計だけを詰めます。

ルールは前回と同じです（読み取り専用・git操作禁止・秘密値禁止・
`scripts/score_forecasts.py` / `snapshot_forecasts.py` / `train_ml_model.py` の実行禁止・
公開APIへの少量GETは歓迎）。書いてよいのは `plan/FORECAST_FREEZE_DEBATE_FINDINGS.md`（回答）と
`plan/FORECAST_REVIEW_PROGRESS.md`（進捗メモ、DEBATE_ 欄）の2つだけです。

---

## 1. 検証で新たに確定した事実（あなたの回答時点では未確定だったもの）

- **重み分布の実測の裏付け**: あなたの 2026-08-22 観測（0.472〜0.711・平均0.598）のうち、
  最小値 gangnam=0.472 が Claude 側の独立GET（9店）と完全一致。08-21 の 0.454〜0.596（平均0.539）
  → 08-22 で平均 +11% 動いたことは両者の記録で整合。**毎晩それだけ配信が揺れている**は事実として確定。
- **`ML_OBJECTIVE` の教訓**: GitHub 管理画面に Repository Variable があっても、
  workflow yml に `${{ vars.X }}` の配線が無ければ**死に設定**（今回あなたが発見）。
  凍結フラグの設計はこの教訓を踏まえる必要があります（Q1）。
- **score ジョブの実行場所**: `.github/workflows/forecast-accuracy-track.yml`（毎朝06:10 JST・GHA）。
  env は 90-102 行目で secrets/vars から明示的に渡すパターン（`OPS_NOTIFY_WEBHOOK_URL` 等）。
- **書き込み箇所**: `scripts/score_forecasts.py:945` が `accuracy/blend_weights.json` を上書き（毎晩・唯一の書き手）。
- **読み手のフォールバック**: `oriental/ml/forecast_service.py:217-290`。ファイル欠損・店舗キー欠損は
  `w_ml=1.0`（純ML）へ graceful fallback。プロセス内キャッシュ TTL 1時間。

---

## 2. オーナー環境の制約（再掲・重要）

- ソロの非エンジニア。**運用手順が増える案は採用されません**。
- Render $7/月 + GHA 無料枠。新規有料サービス不可。
- ロールバックは「環境変数1つ」以下の操作にすること。
- 実装は **1日で終わる規模**を守ること（これはあなた自身のロードマップ1位の条件でもあります）。

---

## 3. Claude 側の実装案（これを批評してください）

```
[3-1] scripts/score_forecasts.py:922-947 の書き込み先を分岐
      freeze = os.environ.get("BLEND_WEIGHTS_FREEZE", "1").strip() == "1"   # 既定=凍結ON
      target = "accuracy/blend_weights_shadow.json" if freeze else "accuracy/blend_weights.json"
      凍結中は配信パス accuracy/blend_weights.json に一切触れない
      （「score成功でも配信weightのhashが不変」を自然に満たす）。
      計算とshadow書き込みは毎晩続ける（＝解除後の答え合わせ材料を貯め続ける）。
      ログに "FROZEN (shadow candidate, served weights unchanged)" を明示。

[3-2] .github/workflows/forecast-accuracy-track.yml に env を明示配線
      BLEND_WEIGHTS_FREEZE: ${{ vars.BLEND_WEIGHTS_FREEZE || '1' }}
      （ML_OBJECTIVE の教訓: コード既定に頼らず yml に配線を書く。
        ロールバックは GitHub の Repository Variable を 0 にする1操作）

[3-3] 初回凍結時のバックアップ
      凍結状態での初回実行時、accuracy/blend_weights.json を
      accuracy/blend_weights_frozen_backup.json へコピー（既にあればスキップ）。
      → Storage 消失時の「自動復旧しない」問題への保険。復旧は backup→serving のコピー1回。

[3-4] 監視（同じPRに含める・小さく）
      既存の監視スクリプト群（scripts/monitor/）と同じ流儀で、
      - 配信ファイル accuracy/blend_weights.json が「存在する」こと（鮮度は見ない。凍結中は古いのが正しい）
      - shadow が 48時間以内に更新されていること（= score ジョブが生きている証明）
      を確認する。

[3-5] テスト3本（tests/test_score_blend_weights.py に追記。既存の _wire_main モックパターンを流用）
      ①凍結時: shadow のみ書かれ、配信パスに書き込みゼロ
      ②BLEND_WEIGHTS_FREEZE=0: 従来どおり配信パスへ
      ③env未設定: 既定=凍結

[3-6] 位置づけ: 時限措置。解除条件は「ロードマップ2位（測定契約=preblend成分の別保存・共通mask採点）が
      本番稼働し、preblend ベースの有効夜が貯まってから」。解除時は毎晩更新には戻さず、
      あなたの V4 案（週次・28有効夜・CI・変化幅±0.05上限）へ移行する。
```

---

## 4. 未決の設計論点（あなたの案と理由を。Claude 案への反対は歓迎です）

**Q1. 凍結フラグの既定値**
Claude 案はコード既定=凍結ON（env未設定でも凍結）+ yml に明示配線。
対案: 既定OFFにして yml だけで ON にする（コードの挙動が env に隠れない）。どちらが事故りにくいか。
ローカルで score を将来手動実行する場合の事故（うっかり配信を上書き）も考慮してください。

**Q2. 凍結する「値」は今日の値で良いか**
凍結は「今夜時点の blend_weights.json をそのまま固定」です。しかしその値自体、汚染された
成績表による最大7夜の産物で、日々±0.06動いていました。
対案候補: (a) 今日の値のまま（Claude 案。全店 0.47〜0.71 の中庸域にあり大外れは無い）
(b) 直近の複数夜のスコアから再計算した平滑値で固定 (c) 全店一律 0.5 に揃えて固定。
(b) は同じ汚染入力を使う点で本質改善にならない、(c) は現状からの変化が大きい、というのが
Claude の見立てですが、あなたの判断は。

**Q3. 新店のフォールバック**
凍結中に新店を出すと、キーが無いので恒久的に w=1.0（純ML）です。
(a) そのまま（現行の未採点店と同じ挙動・Claude 案） (b) キー欠損時は艦隊中央値（≈0.59）を使う
(c) 0.5。ただし (b)(c) は読み手 forecast_service.py の変更を伴い、凍結PRの範囲が広がります。

**Q4. バックアップと監視の設計（3-3, 3-4）**
この形で足りるか。shadow を毎晩書き続けること自体への異論（Storage書き込み1回/晩が増える）や、
より良い置き場所・形式があれば。

**Q5. 解除の条件を今、数値で決めておくべきか**
Claude 案は「測定契約の稼働 + preblend 有効夜の蓄積」と定性的に置いていますが、
あなたの V4 は「14夜未満は凍結・14〜27夜は収縮・28夜で店別」と数値を持っています。
凍結PRの時点で解除条件をコード or 文書にどこまで固定すべきか
（決めすぎると測定契約の設計を先取りしてしまう、決めないと恒久凍結化する）。

**Q6. 凍結と同時にやるべき/やってはいけないことの最終確認**
ロードマップ 2位（測定契約）・3位（as-of backtest + slope整合 + rain除去）との順序・干渉。
特に 3位は schema v7→v8 の3箇所同期と84本再学習を伴うため、凍結中にモデルだけ変わると
「凍結された重みと新モデルの組み合わせ」が未評価のまま配信されます。これは許容ですか、
それとも 3位の出荷時に何か手当てが要りますか。

**Q7. 反対論の最終確認（悪魔の代弁者）**
「凍結しない方が良い」と言える残りの論拠があれば正直に出してください。
例: 自動更新は壊れた基準なりに「大外れした店を翌晩補正する」保険として機能しており、
凍結するとその保険が消える——これは実データで反証できますか。

---

## 5. 回答の形式（`plan/FORECAST_FREEZE_DEBATE_FINDINGS.md`）

日本語で。

1. **§3 Claude 案への批評**: 採用してよい点 / 直すべき点（file:line と理由つき）
2. **Q1〜Q7 への回答**: 各3〜10行。対案には必ず「なぜ Claude 案より良いか」を添える
3. **あなたの最終推奨案一式**: §3 と同じ形式の箇条書きで（Claude 案と同じなら「同じ」でよい）
4. **相違が残った場合**: オーナーがどちらを選ぶべきか、判断材料を非エンジニア向けに3行で

中断時は `plan/FORECAST_REVIEW_PROGRESS.md` に `DEBATE_STATUS` / `DEBATE_NEXT` を書いて再開してください。
