# 外部レビュー 第2ラウンド依頼書（2026-08-21）— 検証結果の共有と、残った論点の決着

宛先: ChatGPT / Codex（第1回レビューを実施したあなた）
前提: あなたの第1回レポート `plan/EXTERNAL_REVIEW_FINDINGS_2026-08-21.md` は受領済みです。

---

## 0. まず結果の共有：あなたの指摘は非常に高精度でした

第1回の15件を、こちらで**1件ずつ実コードに当たって独立検証**しました（開発を手伝っているAI・Claudeが実施）。

| 判定 | 件数 |
|---|---|
| **事実誤認（REFUTED）** | **0件** |
| 指摘どおり（CONFIRMED） | 12件 |
| 一部正しいが誇張・条件付き（PARTIAL） | 2件（F2, F3） |
| 事実だが既知の宿題（KNOWN） | 1件（F9） |

**15件中10件は、こちらが過去5回実施した総点検でも見つけられなかった新規発見**でした。
特に次の3件は利用者に直接見える不具合として、これから修正します（**再度の説明は不要です**）:

- F5「予測取得不能でも実測ピークを予測ハイライトとして表示」— 分岐を実コードで確認、指摘どおり
- F6「レポート一覧が `created_at` 順・`created_at` 表示」— 加えて daily 84行 > limit 50 のため**一部店舗が一覧から恒久的に漏れる**ことも判明（あなたの指摘より深刻）
- F13「トップの主要訴求がSSR時点で `opacity:0`」— 本番HTMLでH1に直接確認

また次の2件も受け入れ、修正対象とします: F1（空・部分snapshotの上書き）、F4（Flask直叩きでレート制限を迂回）。
**F7 は、本番データで検証したところ実測線の時刻一致が200件中0件**で、「分断」どころか**ほぼ完全に不可視**でした。
あなたの指摘はむしろ過小評価でした。

補足: あなたが実行できなかった Python テスト全体は、こちらの環境（lightgbm/scipy あり）で
**1,053件すべて成功**しました。テストコード自体の問題ではありませんでした。

---

## 1. このラウンドでお願いしたいこと

**あなたの結論を撤回させたいのではありません。** むしろ逆で、**こちらの判断が間違っている可能性を潰したい**。
以下の論点は、あなたの結論と、こちらが8/20に別途出した診断結果が**正面から食い違っています**。
どちらが正しいかを、データを見て決着させたいです。遠慮なく我々を否定してください。

ルールは第1回と同じです（`plan/EXTERNAL_REVIEW_2026-08-21.md` の §1）。特に:
**読み取り専用・git push禁止・秘密ファイルを開かない・argparse を持たないスクリプトを実行しない。**

### 新しく渡す資料（第1回で「未確認」とされた穴の穴埋め）

**`plan/_local/ROUND2_METRICS.md` を読んでください。**
GA4 / Search Console の実測値（利用者数・検索表示・クリック・イベント発火・コスト）が入っています。
第1回であなたは「GA4/GSCを確認していないため事業トラクションは未確認。コード量を需要の証拠にしない」と
正しく留保しました。その材料を渡します。
（このファイルは `.gitignore` 済みで公開されません。秘密鍵・個人情報は含みません。集計値のみです。）

---

## 2. 論点A【最重要】事業判断の食い違い — 実データを見た上で再判断してほしい

あなたの結論: **「次の90日は機能追加をせず、信頼性リセットに限定せよ」**

こちらが8/20に出した結論（診断②）: **「ボトルネックは信頼性ではなく検索順位。表示4,093回に対しクリック83回、
平均16位。『知られていない』のではなく『出ているのに選ばれない』。検索評価が店舗ページでなく
日報・週報・一覧に分散しているのが本丸」**

- Q-A1. `plan/_local/ROUND2_METRICS.md` を読んだ上で、**「90日は信頼性リセットに限定」を維持しますか？**
  維持するなら、**利用者37人・リピート5人という母数において、信頼性を上げることがどう成長に接続するのか**を
  説明してください。「信頼できないと再訪しない」は理屈として分かりますが、そもそも訪問がほぼ無い段階で
  再訪率の改善が効くのか、という疑問に答えてください。
- Q-A2. 逆に、こちらの「検索順位が本丸」という結論の弱点を指摘してください。
  （例: 順位を上げても中身が矛盾していれば離脱するだけ、など。あるならデータで）
- Q-A3. **両立案があるなら出してください。**「信頼性のうち、検索順位にも効くものだけ先にやる」という
  順序付けが可能なら、それが最善に見えます。F13（トップのH1が `opacity:0`）は
  LCP経由でSEOにも効く可能性があり、まさにその交差点かもしれません。あなたはどう見ますか。

---

## 3. 論点B【最重要】予測の扱いの食い違い

あなたの提言: **「v2 shadow・自己更新blend・複雑なChampion/Challenger自動昇格を止め、
raw/served を分けた同一期間評価ができるまで固定baselineを使え」**

こちらの診断①（8/20）の結論: **「切替も重み調整もしない。観測継続。ただし計器を2つ足す」**
- 背景: 2026-07-10 に「テンプレ法(v2)が本番を33%圧倒」と判定したが、**その判定の前日に本番へ
  ベースライン・ブレンドが投入されており、33%の大半は既に回収済み**だった（判定に使われた誤差11.88 ≒
  ブレンド前7晩の実測平均11.87で数値一致）。
- クリーンな8晩（7/10〜7/17）では **v2は本番より+2.1%良いだけ**。平日夜は+7.4%だが、
  **利用者が最も見る金土祝前夜はほぼ互角**（本番12.07 vs v2 12.37）。店舗別では本番が勝つ店も11店ある。
- お盆（8/11-16）と夏休みのデータは**事故欠測でゼロ**＝特殊期間の優劣は測れていない。
- 現行の重み `w_ml` は42店平均0.539、範囲 [0.454, 0.596]。クランプは [0.15, 0.9]。

あなたが F9 で指摘した「配信値を純ML誤差として再投入する自己参照ループ」は、こちらも診断①で
独立に発見しており（"self-flattery loop"）、対策として次の2つを計画済み・**未実装**です:
1. snapshot に **blend適用スロット率**を記録する（「今夜のliveは何%ブレンドだったか」を可視化）
2. 既に毎晩記録されているのに誰も見ていない **peak_time_hit30 / ghost_index / band_coverage** を週次集計する

- Q-B1. 実際の履歴を見てください（`accuracy/scores/summary.json` と `accuracy/blend_weights.json` の
  読み取りコードは `scripts/score_forecasts.py` にあります。**スクリプトの実行は禁止**なので、
  ファイルを直接取得する小さな読み取りコードを書くか、コードから構造を読み取る形でお願いします）。
  **w_ml がクランプの端（0.15 や 0.9）まで振れた夜は実在しますか？** 実在しないなら、
  あなたが指摘した発散シナリオは理論上のもので、実運用では収縮していることになります。
- Q-B2. 上記の**計器2つで、自己参照の測定穴は塞がりますか？** それとも、計器を足しても
  「純ML成分の性能が測れない」という本質は残るので、やはり自動更新を凍結すべきですか。
- Q-B3. **凍結した場合の損失**をどう見積もりますか。現在の本番は約46%がベースライン成分で、
  ブレンド導入で誤差が 11.87 → 9.17 に改善した実績があります。凍結＝この改善を止めることではないか、
  という反論に答えてください。

---

## 4. 論点C【食い違い】日次・週次レポートの生成停止提言

あなたの提言: **「Daily 2便×42店とWeeklyの生成AI文章量産を止めよ。現在人数と単純な定型要約で十分」**

こちらの実測（診断②）: **日報+週報が検索表示の35.5%（約1,454回）を占めるがクリックは8件。**
これに対しこちらは「生成を止める」のではなく **「週報を noindex にして検索評価を店舗ページへ集約する」**
という対処を8/20に出荷済みです（commit 17e815f。日報は7月から既に noindex）。

- Q-C1. **生成そのものを止めた場合**、(a) 検索での不利、(b) サイトの鮮度・情報量の低下 を
  どう見積もりますか。noindex 化で十分ではない理由があれば教えてください。
- Q-C2. 生成コストは**実質ゼロ**です（オーナーPCのローカルLLM・API課金なし・電気代のみ）。
  それでも止めるべきですか。止める理由は「コスト」ではなく「運用の複雑さ」という理解で合っていますか。
- Q-C3. 折衷案として「生成頻度を落とす（1日2便→1便、週報は隔週）」はどう評価しますか。

---

## 5. 論点D【判定の食い違い】F2・F3 を PARTIAL とした理由への再反論

こちらは F2（収集の daemon thread / 202応答）と F3（永続化失敗でも成功扱い）を
**重大度「高」→「中」に下方修正**しました。理由:

1. `.github/workflows/check-collection-heartbeat.yml` が**2時間ごと（24時間体制）**に
   `logs` テーブルの最新タイムスタンプを直接照会しており、夜間窓は30分閾値、店舗別は3日閾値で検知する。
   ＝タスクの自己申告に依存しない独立の鮮度監視が既にある。
2. 収集は**5分周期**なので、単発の途中終了は次の周期で自己修復する。
3. 202を返す非同期化は `67434a2`（2026-03-28）で **gunicorn の timeout 対策として意図的に導入**された
   設計であり、Procfile のコメントにもその旨が明記されている。
4. `insert_supabase_log` が `HAS_SUPABASE=False` のとき `True` を返すのは、**docstring に明記された
   開発時フォールバック**（ローカル開発でクラッシュさせないため）。

- Q-D1. これらを踏まえてなお**重大度「高」を維持しますか？** 維持するなら、
  **上記のheartbeat監視をすり抜ける具体的なシナリオ**を1つ、時系列で示してください。
- Q-D2. こちらは「真の欠陥は `oriental/routes/tasks.py` が `collect_all_once()` の `fail` 件数を
  一切検査せず `status=\"completed\"` / `ok:true` にする点に集約される」と判断しました。
  この要約で過不足ないですか。

---

## 6. 論点E【要・根拠提示】F14 の波及範囲

F14（週報の `target_date` がUTC基準で火曜日付になる）について、あなたは
「**一覧の並び順と監視対象日もずれる**」と書きましたが、こちらの検証では:
- 一覧のソートは `created_at`（`target_date` ではない）
- 監視スクリプト（`scripts/monitor/check_weekly_published.py`）は `target_date` を参照していない

となり、**波及部分の裏取りができませんでした**（日付ラベルが1日ずれる表示バグ自体はCONFIRMED）。

- Q-E1. 「並び順・監視対象日もずれる」の**根拠となるコードの file:line** を示してください。
  こちらの読み落としなら訂正します。示せない場合は、その部分は取り下げてください。

---

## 7. 論点F【定量化の依頼】F13 の影響を数値にしたい

F13（トップの主要訴求が `opacity:0` でSSRされる）について、こちらは
**「SEOの掲載順位が低い」という診断②の結論と因果でつながる可能性がある**と見ています。

- Q-F1. **PageSpeed Insights / Lighthouse で LCP を実測**できますか（`https://www.meguribi.jp/`）。
  可能なら数値を出してください。
- Q-F2. 同じ `FadeIn` パターンが、**SEO上の主要着地点である店舗詳細ページ（`/store/[id]`）や
  エリアページ（`/area/[area]`）でも H1 を隠していないか**、横展開で調査してください。
  隠しているなら、それが「表示はあるのにクリックされない」現象にどれだけ効いていそうか、見解をください。

---

## 8. 論点G【利用実績ゼロの機能をどうするか】

比較ページ（`/compare`）は F7・F10 で2件の重大な指摘を受けましたが、
**GA4の実測では `compare_add_store` イベントが過去90日で0件**です（`plan/_local/ROUND2_METRICS.md` 参照）。
料金シミュレーターも90日で1件、お気に入りも1件です。

- Q-G1. 比較ページについて、(a) 修正する / (b) グラフだけ隠す（あなたの第1回提言） / (c) 機能ごと撤去 の
  どれを推しますか。**利用実績ゼロという事実を前提に**判断してください。
- Q-G2. 同様に、料金シミュレーター・お気に入り・マイページについても、
  「残す / 隠す / 消す」の判断をください。**機能を減らすこと自体が信頼性リセットに寄与する**という
  観点があるなら、それも含めて。

---

## 9. 論点H【実装順序】あなたの提言とこちらの未実装案の統合

あなたの提言する「信頼性リセット」（generation_id / expected_slugs / end-to-end canary / 通知1本化 / 通知訓練）と、
こちらが `docs/FAILURE_MAP.md` で挙げた**未実装の監視3案**は、目的が重なっています:

1. 通知の終着点をLINEに一本化（LINE送信のコードは既に3箇所に存在。移植のみ・半日）
2. PC生存ハートビート＋「空実行」検知を既存の2時間毎監視に相乗り（1日）
3. 週次LINEダイジェストに運用ステータス欄を追加（半日）＝「毎週来るはずの便りが来ない」で監視の死も拾う

- Q-H1. **これらとあなたの提言を統合した、最小の実装順序**を出してください。
  「まずこれ1つだけやれば、8月に起きた1ヶ月事故の再発は防げる」という**単一の施策**があるなら、それを名指しで。
- Q-H2. F1（空・部分snapshotの上書き）の修正と、上記の監視追加は**どちらを先にやるべきですか**。

---

## 10. 出力形式

`plan/EXTERNAL_REVIEW_ROUND2_FINDINGS.md` に日本語で出力してください。構成:

1. **論点A〜Hへの回答**（各論点ごとに「結論 → 根拠 → 確信度」。こちらの主張を否定する場合は、
   否定の根拠を必ず file:line か実データで示してください）
2. **第1回の判定を修正する点があれば、その訂正**（F2/F3/F14 について、こちらの反論を受けて
   考えを変えたなら、正直にそう書いてください。変えないならその理由を）
3. **最終提言（1ページ）**: 実データを見た上での「次の90日でやるべきこと」の最終版。
   優先順に3つまで。それぞれ「なぜそれか」「何をもって成功とするか」を1行ずつ。

進捗は第1回と同じく `plan/EXTERNAL_REVIEW_PROGRESS.md` に記録してください
（`STATUS` を `RUNNING` にし、`NEXT` に現在の論点を書く。中断・再開のプロトコルは第1回と同じです）。

---

## 11. 最後に

第1回のレビューは、こちらが5回の総点検で見つけられなかった穴を10件見つけました。
率直に言って、**外部の目を入れて正解でした**。第2ラウンドも遠慮は不要です。
こちらの診断（Claudeが出した結論）が間違っているなら、そう言ってください。
