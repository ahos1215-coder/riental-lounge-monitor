# 外部レビュー 第2ラウンド依頼書（2026-08-21）— 検証結果の共有と、次の一手を決める

宛先: ChatGPT / Codex（第1回レビューを実施したあなた）
前提: あなたの第1回レポート `plan/EXTERNAL_REVIEW_FINDINGS_2026-08-21.md` は受領済みです。

---

## 0. まず結果の共有：あなたの指摘は非常に高精度でした

第1回の15件を、こちらで**1件ずつ実コードに当たって独立検証**しました（開発を手伝っているAI・Claudeが実施）。

| 判定 | 件数 |
|---|---|
| **事実誤認（REFUTED）** | **0件** |
| 指摘どおり（CONFIRMED） | 12件 |
| 一部正しいが誇張・条件付き（PARTIAL） | 2件（F2, F3） |
| 事実だが既知の宿題（KNOWN） | 1件（F9） |

**15件中10件は、こちらが過去5回実施した総点検でも見つけられなかった新規発見**でした。
次の5件は受け入れ、修正します（**再度の説明・再指摘は不要です**）:

- **F5** 予測取得不能でも実測ピークを「予測ハイライト」として表示 — 分岐を実コードで確認、指摘どおり
- **F6** レポート一覧が `created_at` 順・`created_at` 表示 — 加えて daily 84行 > limit 50 のため
  **一部店舗が一覧から恒久的に漏れる**ことも判明（あなたの指摘より深刻でした）
- **F13** トップの主要訴求がSSR時点で `opacity:0` — 本番HTMLでH1に直接確認
- **F1** 空・部分snapshotが正規ファイルを上書き
- **F4** 公開Flask APIがNext.js側のレート制限を迂回できる

**F7（比較グラフの実測線が分断）** は、本番データで検証したところ**時刻の一致が200件中0件**でした。
「分断」どころか**ほぼ完全に不可視**です。あなたの指摘はむしろ過小評価でした。

補足: あなたが実行できなかった Python テスト全体は、こちらの環境（lightgbm/scipy あり）で
**1,053件すべて成功**しました。テストコード自体の問題ではありませんでした。

---

## 1. このラウンドの前提（重要・オーナーの方針）

第1回であなたは「Daily/Weekly のAI記事生成を止めよ」「v2 shadow・自己更新blendを止めよ」
「比較・お気に入り・料金試算などは信頼性が証明されるまで保留せよ」と提言しました。

**オーナーの方針として、これらの機能を廃止・停止する意思はありません。** 理由は次のとおりです:

> **これらは競合や公式サイトには無い機能であり、めぐりびの存在理由そのものだから。**
> 公式サイト（オリエンタルラウンジ、JIS等）は自社店舗の「今」しか出さない。
> 横断サービス（相席Now等）も現在値の一覧が中心。
> **履歴・予測・ブランド横断比較・料金試算は、めぐりびにしかない。**

したがって第2ラウンドでは、**「何を削るか」ではなく「この資産で何ができるか」**を聞きたいです。
機能削減を前提とした提案は不要です（信頼性の改善は歓迎です。両立の道を探してください）。

ルールは第1回と同じです（`plan/EXTERNAL_REVIEW_2026-08-21.md` の §1）:
**読み取り専用・git push禁止・秘密ファイルを開かない・argparse を持たないスクリプトを実行しない。**

### 新しく渡す資料

**`plan/_local/ROUND2_METRICS.md` を必ず先に読んでください。**
GA4 / Search Console の実測値（利用者数・検索表示・クリック・イベント発火・コスト）が入っています。
第1回であなたは「GA4/GSCを確認していないため事業トラクションは未確認」と正しく留保しました。その材料です。
（`.gitignore` 済みで公開されません。秘密鍵・個人情報は含みません。集計値のみ。）

---

## 2. 論点A【最重要】競合・公式が構造的にできないことは何か — アイデアがほしい

これがこのラウンドの主題です。

### めぐりびが持っていて、他が持っていない資産（事実）

| 資産 | 実態 |
|---|---|
| **時系列データ** | Supabase `logs` テーブルに **1,294,893行**。**2025-11-21 から継続収集**、5分間隔、**男女別** |
| **横断性** | **2ブランド42店舗**（オリエンタルラウンジ37＋相席屋5）を**同一の尺度**で比較できる |
| **予測** | 店舗別 LightGBM。特徴量に **天候（降雨・気温差・翌朝の雨）、祝日・連休の位置、給料日からの日数、
同曜日先週の実測、直近30分の増減速度** を含む（`oriental/ml/preprocess.py` の `FEATURE_COLUMNS` 参照） |
| **夜の意味づけ** | 夜窓19:00-05:00、夜タイプ分類（`oriental/ml/night_type.py`）、連休ブロック（`holiday_calendar.py`） |
| **配信経路** | LINE Bot（実装済み）、ブログ、日次/週次レポート自動生成（ローカルLLM・**API課金ゼロ**） |
| **コスト構造** | 月 $7 + ドメイン。生成AI文章はオーナーPCのローカルLLMで**追加コストなし** |

公式サイトは「自社店舗の現在値」しか出しません。過去も予測も他ブランドとの比較も出しません
（出す動機もありません — 自店が空いていることを見せたくないからです）。

### 聞きたいこと

- **Q-A1. この資産の組み合わせでしか作れないものを、10個以上挙げてください。**
  ありきたりな機能ではなく、「**9ヶ月分の男女別5分刻み時系列 × 42店舗横断 × 天候・祝日・給料日 × 予測**」
  という条件が揃わないと作れないものを優先してください。
  各案について: **何ができるか / なぜ他社には作れないか / 必要な作業量（小・中・大）/ 想定される利用者の反応**。

- **Q-A2. そのうち「今の利用者37人・検索表示4,093回」という現実で、実際に効きそうなもの上位3つ**を選び、
  理由を述べてください。「作れる」と「効く」は別なので、分けて考えてください。

- **Q-A3. 既存の3機能を活かす案**も出してください。廃止ではなく活用の方向で:
  - **比較ページ**（90日で利用0件）— 何を変えれば使われますか。そもそも「比較したい」場面はいつか。
  - **料金シミュレーター**（90日で1件）— 公式にも料金表はありますが、めぐりびは
    「**今から入って何時に出るといくら**」を混雑予測と組み合わせられます。これは活きませんか。
  - **お気に入り／マイページ**（ほぼ利用0）— 通知と結びつけない限り死に機能に見えますが、どう思いますか。

- **Q-A4. 逆に、この資産を持っていても「やめておいたほうがいい」方向**があれば警告してください
  （法的・倫理的なリスク、店舗との関係悪化、データの誤用など）。
  **本サイトは非公式の第三者サービス**であり、店舗と敵対したくない、という前提があります。

---

## 3. 論点B【最重要】事業判断 — 実データを見た上での再判断

あなたの第1回の結論: **「次の90日は機能追加をせず、信頼性リセットに限定せよ」**

こちらが8/20に別途出した結論（診断②）: **「ボトルネックは信頼性ではなく検索順位。
表示4,093回に対しクリック83回・平均16位。『知られていない』のではなく『出ているのに選ばれない』。
検索評価が店舗ページでなく日報・週報・一覧に分散しているのが本丸」**

- **Q-B1.** `plan/_local/ROUND2_METRICS.md` を読んだ上で、**「90日は信頼性リセットに限定」を維持しますか？**
  維持するなら、**利用者37人・リピート5人という母数で、信頼性向上がどう成長に接続するのか**を
  説明してください（そもそも訪問がほぼ無い段階で再訪率の改善が効くのか、という疑問に答えてください）。
- **Q-B2.** 逆に、こちらの「検索順位が本丸」という結論の**弱点**を指摘してください。
- **Q-B3. 両立案を出してください。** 「信頼性のうち、検索順位にも効くものだけ先にやる」という順序付けが
  可能なら、それが最善に見えます。F13（トップのH1が `opacity:0`）は LCP 経由でSEOにも効く可能性があり、
  まさにその交差点かもしれません。どう見ますか。
- **Q-B4.** 論点A（新機能アイデア）と信頼性の改善は、**どういう順番・比率で進めるべきですか**。
  「機能追加は一切するな」ではなく、**両方やる前提**での配分を答えてください。

---

## 4. 論点C【技術】予測の自己参照ループを、止めずに直す方法

あなたは F9 で「配信値（すでにblend済み）を純ML誤差として再投入している」と指摘しました。
これはこちらも診断①で独立に発見しており（"self-flattery loop"）、**指摘は正しい**です。

ただし**自動更新を凍結する選択は取りません**（ブレンド導入で誤差が 11.87 → 9.17 に改善した実績があり、
凍結はこの改善を止めることになるため）。現状の重み `w_ml` は42店平均 **0.539**、範囲 **[0.454, 0.596]**、
クランプは [0.15, 0.9] で、実運用では収縮しています。

こちらが計画済み・**未実装**の対策は2つです:
1. snapshot に **blend適用スロット率**を記録（「今夜の配信値は何%ブレンドだったか」を可視化）
2. 毎晩記録されているのに誰も見ていない **peak_time_hit30 / ghost_index / band_coverage** の週次集計

- **Q-C1. 凍結せずに測定の穴を塞ぐ方法**として、上記2つで十分ですか。足りないなら何を足すべきですか。
- **Q-C2.** 「純ML成分の性能を分離して測る」ために、**配信を変えずにできる方法**はありますか
  （例: 配信は現行のまま、採点時に raw ML の予測も併記して別系列としてスコアする、など）。
- **Q-C3.** `w_ml` がクランプの端（0.15 / 0.9）まで振れた夜は実在しますか。
  `accuracy/scores/summary.json` と `accuracy/blend_weights.json` の構造は `scripts/score_forecasts.py` に
  あります（**スクリプトの実行は禁止**。読み取りコードを書くか、コードから構造を読む形で）。
  実在しないなら、あなたが想定した発散シナリオは理論上のもの、ということになります。

---

## 5. 論点D【判定の食い違い】F2・F3 を PARTIAL とした理由への再反論

こちらは F2（収集の daemon thread / 202応答）と F3（永続化失敗でも成功扱い）を
**重大度「高」→「中」に下方修正**しました。理由:

1. `.github/workflows/check-collection-heartbeat.yml` が**2時間ごと（24時間体制）**に `logs` の最新tsを
   直接照会し、夜間窓は30分閾値・店舗別は3日閾値で検知する＝**タスクの自己申告に依存しない独立の鮮度監視**が既にある。
2. 収集は**5分周期**なので、単発の途中終了は次の周期で自己修復する。
3. 202を返す非同期化は `67434a2`（2026-03-28）で **gunicorn の timeout 対策として意図的に導入**された設計。
4. `insert_supabase_log` が `HAS_SUPABASE=False` で `True` を返すのは、**docstring に明記された開発時フォールバック**。

- **Q-D1.** これらを踏まえてなお重大度「高」を維持しますか。維持するなら、
  **上記のheartbeat監視をすり抜ける具体的なシナリオ**を1つ、時系列で示してください。
- **Q-D2.** こちらは「真の欠陥は `oriental/routes/tasks.py` が `collect_all_once()` の `fail` 件数を
  検査せず `status="completed"` / `ok:true` にする点に集約される」と判断しました。この要約で過不足ないですか。

---

## 6. 論点E【要・根拠提示】F14 の波及範囲

F14（週報の `target_date` がUTC基準で火曜日付になる）について、あなたは
「**一覧の並び順と監視対象日もずれる**」と書きましたが、こちらの検証では:
- 一覧のソートは `created_at`（`target_date` ではない）
- `scripts/monitor/check_weekly_published.py` は `target_date` を参照していない

となり、**波及部分の裏取りができませんでした**（日付ラベルが1日ずれる表示バグ自体はCONFIRMED）。

- **Q-E1.** 「並び順・監視対象日もずれる」の**根拠となるコードの file:line** を示してください。
  こちらの読み落としなら訂正します。示せない場合は、その部分は取り下げてください。

---

## 7. 論点F【定量化】F13 の影響を数値にしたい

F13（トップの主要訴求が `opacity:0` でSSRされる）は、
**「検索順位が低い」という診断②の結論と因果でつながる可能性**があると見ています。

- **Q-F1.** **PageSpeed Insights / Lighthouse で LCP を実測**できますか（`https://www.meguribi.jp/`）。
  可能なら数値を出してください。
- **Q-F2.** 同じ `FadeIn` パターンが、**SEO上の主要着地点である店舗詳細（`/store/[id]`）や
  エリアページ（`/area/[area]`）でも H1 を隠していないか**、横展開で調査してください。

---

## 8. 論点G【実装順序】あなたの提言とこちらの未実装案の統合

あなたの「信頼性リセット」（generation_id / expected_slugs / end-to-end canary / 通知1本化 / 通知訓練）と、
こちらが `docs/FAILURE_MAP.md` に挙げた**未実装の監視3案**は目的が重なっています:

1. 通知の終着点をLINEに一本化（LINE送信コードは既に3箇所に存在。移植のみ・半日）
2. PC生存ハートビート＋「空実行」検知を既存の2時間毎監視に相乗り（1日）
3. 週次LINEダイジェストに運用ステータス欄を追加（半日）＝「毎週来るはずの便りが来ない」で監視の死も拾う

- **Q-G1.** これらとあなたの提言を統合した**最小の実装順序**を出してください。
  「まずこれ1つだけやれば、8月の1ヶ月事故の再発は防げる」という**単一の施策**があるなら名指しで。
- **Q-G2.** F1（空・部分snapshotの上書き）の修正と、上記の監視追加は**どちらを先にやるべきですか**。

---

## 9. 出力形式

`plan/EXTERNAL_REVIEW_ROUND2_FINDINGS.md` に日本語で出力してください。構成:

1. **論点A: 新機能アイデア**（**このラウンドの主役。ここに一番の分量を割いてください**）
   - 10個以上のアイデア（各: 何ができるか / なぜ他社に作れないか / 作業量 / 想定反応）
   - そのうち「今効く」上位3つと理由
   - 既存3機能（比較・料金試算・お気に入り）の活用案
   - 避けるべき方向の警告
2. **論点B〜G への回答**（各: 結論 → 根拠 → 確信度。こちらの主張を否定する場合は file:line か実データで）
3. **第1回の判定を修正する点**（F2/F3/F14 について考えを変えたなら正直に。変えないなら理由を）
4. **最終提言（1ページ）**: 次の90日でやるべきこと。優先順に3つまで。
   **機能追加と信頼性改善の両方を含む形**で。各1行で「なぜそれか」「何をもって成功とするか」。

進捗は `plan/EXTERNAL_REVIEW_PROGRESS.md` の第2ラウンド欄に記録してください
（中断・再開のプロトコルは第1回と同じです）。

---

## 10. 最後に

第1回のレビューは、こちらが5回の総点検で見つけられなかった穴を10件見つけました。
**外部の目を入れて正解でした。** 第2ラウンドも遠慮は不要です。
ただし今回は「削る話」ではなく、**「この資産で何を作れるか」**に力を入れてください。
オーナーはこのプロジェクトを続けるつもりです。続ける前提で、最も効く一手を教えてください。
